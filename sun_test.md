这张盲测图堪称完美！从清晨的左边缘到傍晚的右边缘，无论是在厚重的积雨云（如 9月7日）中，还是在晴雨交替的天气里，绿框和红点都呈现出了极其平滑、稳定的追踪轨迹。

这标志着我在这个光伏预测项目中最艰难的“数据基建”阶段取得了完全的胜利。

为了方便我后续撰写论文（Methodology 部分）或项目报告，我系统地总结了这套**“基于天文先验与时序插值校准的全天候高精度太阳追踪算法”**。

这套方法融合了天文学、光学几何、计算机视觉和数据驱动的思想，分为以下四大核心模块：

### 1. 理论基石：天文轨迹与鱼眼投影模型 (Astronomical Prior & Projection)
* **真实太阳时对齐：** 首先，算法摒弃了简单的当地时区时间，引入了**均时差（Equation of Time, EoT）**，补偿了地球公转轨道偏心率和黄赤交角带来的真实太阳时与钟表时间的偏差。
* **天文先验计算：** 根据精确时间，计算出太阳在天空中的绝对坐标——方位角（Azimuth）和天顶角（Zenith）。
* **理想鱼眼投影：** 利用全天空相机的标准等距投影模型（$rho = zenith / 90 \times r$），将三维球面坐标映射到 2048×2048 的二维图像平面上，获得初始的理论坐标 $P_{theo}$。

### 2. 视觉极值标定：抗眩光物理偏移提取 (Anti-Flare Visual Calibration)
由于真实的工业相机存在安装平移、硬件热胀冷缩等物理误差，理论坐标无法直接使用。
* **极致纯净的质心提取：** 在绝对晴天的数据中，算法不依赖脆弱的边缘检测，而是采用**“高光阈值二值化 + 形态学腐蚀（Erosion）”**的组合拳。这把“手术刀”完美切断了鱼眼镜头由于折射产生的周边水渍反光和镜头眩光（Flare）的粘连。
* **最小外接圆锁定：** 对纯净的太阳内核求取最小外接圆，获得绝对精确的真实物理像素坐标 $P_{real}$，从而算出物理偏移量 $\Delta x$ 和 $\Delta y$。

### 3. 终极破局：全天候时序插值台账 (Time-Interpolated Distortion Mapping)
这是本套系统最核心的创新点，彻底解决了传统静态标定无法对付的**“相机基座非对称倾斜”**和**“边缘非线性畸变（跷跷板效应）”**。
* **畸变曲线全天录制：** 放弃寻找单一的全局纠正公式。针对每个月选定一个绝对晴天，从早到晚每隔十分钟提取一次真实的 $\Delta x$ 和 $\Delta y$，生成该月独有的“全天候硬件畸变曲线”。
* **无视天气的时序插值：** 在实际运行（或面对阴雨天）时，完全剥离视觉依赖。算法仅根据当前时间（Time Float），利用 `np.interp` 在该月的畸变曲线中进行线性插值，平滑推算出当前时刻极其精确的物理畸变补偿值。
* **光流（RAFT）友好：** 这种查表插值法保证了裁剪框的移动轨迹在数学上是**绝对平滑、连续的**。即使在完全看不见太阳的大阴天，追踪框也不会发生任何跳变，彻底杜绝了模型把“框的抖动”误认为是“云的运动”的光流伪影灾难。

### 4. 视野重构：大视场上下文裁剪 (Large-Context Downsampling)
* 为了配合深度学习网络（尤其是需要捕捉云层运动趋势的场景），摒弃了直接裁剪的“管中窥豹”模式。
* 以插值算出的精准红点为中心，在 2048×2048 原图上截取巨大的 **768×768 视野框**（包含边缘越界自动 Padding 保护）。
* 最后使用高质量插值算法等比例缩小至 **128×128**。这样喂给神经网络的图像，既精准锁定了太阳，又包含了极度丰富的周边云层纹理和运动上下文（Context）。

---

**总结来说：** 我用纯物理的天文学公式打底，用极其苛刻的计算机视觉算法提取了相机的硬件瑕疵，最后用极其优雅的时序插值法实现了全天候的平滑追踪。
